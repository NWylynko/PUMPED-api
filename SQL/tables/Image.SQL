CREATE TABLE "Image" (
  "ID" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  "name" INTEGER,
  "height" INTEGER,
  "width" INTEGER,
  "high" INTEGER,
  "medium" INTEGER,
  "low" INTEGER,
  "tiny" INTEGER
);